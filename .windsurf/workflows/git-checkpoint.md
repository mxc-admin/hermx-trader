---
description: Quick mid-session checkpoint commit. Saves all current changes with a timestamped message.
---

# /git-checkpoint — Quick Save

// turbo
Run: `git add -A && git commit -m "checkpoint: $(date +%Y-%m-%d_%H:%M)"`
Report the commit hash.
