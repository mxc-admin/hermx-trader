# Core runtime deps (stdlib-heavy; only third-party listed here)
# CCXT exchange transport for multi-venue execution/polling
ccxt>=4.0.0,<5
# alert schema enforcement (lazy import in webhook_receiver; fails open if absent)
jsonschema>=4.23.0,<5
